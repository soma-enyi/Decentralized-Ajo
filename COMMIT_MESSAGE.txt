feat: Extract dashboard to dedicated route with real-time stats

- Create dedicated /dashboard route with authentication guard
- Implement /api/stats endpoint for aggregated statistics
  - Active circles count
  - Total contributed amount (completed contributions)
  - Total members across all circles
  - Total withdrawn amount
- Add DashboardStats component with skeleton loading states
- Integrate SWR for efficient data fetching with auto-refresh
- Simplify homepage to landing page only with auto-redirect
- Fix missing imports in circles API route

Stats are calculated using Prisma transactions for consistency
and refresh automatically every 30 seconds. Unauthenticated
users are redirected to login when accessing /dashboard.

Closes #[issue-number]
